apiVersion: apps/v1
kind: Deployment
metadata:
  name: sheetmusic-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sheetmusic-app
  template:
    metadata:
      labels:
        app: sheetmusic-app
    spec:
      containers:
        - name: sheetmusic-app
          image: 870026572518.dkr.ecr.us-east-1.amazonaws.com/sheetmusic-app:latest
          ports:
            - containerPort: 8000
